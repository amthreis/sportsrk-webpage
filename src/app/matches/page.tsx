"use client";
import LastMatches from "@/cmp/last-matches";

export default function Home() {
    return (
        <main className="flex flex-col items-center px-[0px] py-8 w-full gap-3">
            <LastMatches/>
        </main>
    );
}
